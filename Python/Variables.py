price = 10 # here price is a variable
print(price) # here we do not add quotes around price because we want to print the value stored in the variable
name = 'John Smith' # here name is a variable
age = 20 # here age is a variable
is_patient = True # here is_patient is a variable
is_new = True # here is_new is a variable
print("we checked in a patient named " + name + "He is " + str(age) + " years old and is a new patient: " + str(is_new))