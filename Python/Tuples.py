list = [ 1, 2, 3, 4, 5 ] # we use [] to define a list
tuple = ( 1, 2, 3, 4, 5 ) # we use () to define a tuple
print(tuple[0])
# tuples[0] = 10 # this will raise an error because tuples are immutable. tuples cannot be changed after they are created. its useful for data that should not be changed.
